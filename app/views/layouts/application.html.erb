<!DOCTYPE html>
<html>
  <head>
    <title>MaheshHotel1</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
    <% if flash[:notice] %>
    <div class="bg-green-200 text-green-800 p-3 mb-4 rounded-md"><%= flash[:notice] %></div>
  <% elsif flash[:alert] %>
    <div class="bg-red-200 text-red-800 p-3 mb-4 rounded-md"><%= flash[:alert] %></div>
  <% end %>
      <%= yield %>
    </main>
  </body>
</html>
