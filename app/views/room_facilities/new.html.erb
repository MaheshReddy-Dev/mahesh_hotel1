<%= turbo_frame_tag "new_room_facility" do  %>
<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New room facility</h1>

  <%= render "form", room_facility: @room_facility %>

  <%= link_to 'Back to login page', root_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
<% end %>